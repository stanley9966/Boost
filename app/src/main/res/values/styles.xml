<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WinColorTheme" parent="AppTheme">
        <item name="android:background">@color/lighterGreen</item>
        <item name="android:textColor">@color/darkerGreen</item>

        <item name="android:textSize">30sp</item>
    </style>

    <!-- Maybe make theme for red, although if only using for cardview it's unnecessary -->

</resources>
